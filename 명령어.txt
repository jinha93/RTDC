----------docker를 이용한 app 기동----------
1. jar build
- 명령어 : ./gradlew build
- 명령어 실행 위치 : /rtdc/app

2. docker image build & docker container run
- 명령어 : docker-compose up 
- 옵션 : 
	--build : Build images before starting containers.
	--force-recreate : Recreate containers even if their configuration and image haven't changed.
	-d, --detach : Detached mode: Run containers in the background, print new container names. Incompatible with --abort-on-container-exit.
- 명령어 실행 위치 : /rtdc


----------db = docker, app = local 환경에서 기동----------(
1. databse docker container run
- 명령어 : docker run database
- 명령어 실행 위치 : /rtdc

2-1. application run
- 명령어 : ./gradlew bootrun
- 명령어 실행 위치 : /rtdc/app

2-2. sts toll 에서 springboot application run
- 프로젝트 우클릭
- Run As 클릭
- Spring Boot App 클릭


※vs code에서 2-1 방법이 실행되지 않아, 2-2 방법으로 진행 중